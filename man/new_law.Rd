% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/law.R
\name{new_law}
\alias{new_law}
\alias{assume}
\alias{check_law}
\alias{format_check_result}
\alias{expect_law}
\title{Define and check a generative law}
\usage{
new_law(name, generators, holds)

assume(condition)

check_law(
  law,
  tests = getOption("s7contract.tests", 100L),
  seed = getOption("s7contract.seed", 1L),
  shrinks = getOption("s7contract.shrinks", 100L),
  discards = getOption("s7contract.discards", 100L),
  max_size = getOption("s7contract.max_size", 100L)
)

format_check_result(x)

expect_law(
  law,
  tests = getOption("s7contract.tests", 100L),
  seed = getOption("s7contract.seed", 1L),
  shrinks = getOption("s7contract.shrinks", 100L),
  discards = getOption("s7contract.discards", 100L),
  max_size = getOption("s7contract.max_size", 100L)
)
}
\arguments{
\item{name}{Non-empty description of the law.}

\item{generators}{Uniquely named non-empty list of generators.}

\item{holds}{Function accepting the generated arguments and returning one
non-missing logical value.}

\item{condition}{Scalar logical precondition.}

\item{law}{A law created by \code{new_law()}.}

\item{tests}{Number of passing cases required.}

\item{seed}{Deterministic local random seed. The caller's RNG kind and state
are restored after the run.}

\item{shrinks}{Maximum number of candidate shrink evaluations.}

\item{discards}{Maximum number of discarded generated cases.}

\item{max_size}{Maximum size passed to generators.}

\item{x}{A result returned by \code{check_law()}.}
}
\value{
\code{new_law()} returns an S7 law. \code{check_law()} returns an S7 check
result. \code{expect_law()} returns one \code{tinytest} result. \code{assume()} returns
invisibly when its condition is true and otherwise discards the case.
}
\description{
A law is a named, universally quantified claim sampled over explicitly
supplied generators. \code{check_law()} is test-framework neutral and returns a
structured S7 result. \code{expect_law()} adapts that result to one tinytest
expectation, regardless of how many generated cases were checked.
}
\details{
Runs use Mersenne-Twister, Inversion normals, and Rejection sampling,
independently of the caller's RNG kind. Box-Muller callers are rejected before
any RNG state is changed because R does not expose their cached normal draw.
Replay requires unchanged generator/law code, run parameters, and compatible
R/package versions; generators and laws must not depend on external mutable
state or change the RNG configuration. The result's \code{parameters} list records
all run arguments except \code{law}, for use with \code{do.call(check_law, ...)}.

Shrinking is an ordered search, not a guarantee of a global minimum. The
counterexample's \code{minimal} field holds the smallest example found. A result's
\code{shrink_status} is \code{"complete"} when no immediate child preserves the
failure, \code{"budget"} when the evaluation limit stopped the search (including
zero), \code{"error"} if constructing candidates failed, or \code{"not_needed"} when no
counterexample was found. A shrinking error or warning is stored separately
in \code{shrink_condition}; the original and last failing examples are retained.
Generator warnings and errors terminate the run with status \code{"error"}.
Warnings or errors from \code{holds} are counterexamples.

In a tinytest file, call \code{tinytest::using(s7contract)} before calling
\code{expect_law()}. This activates tinytest's supported extension capture so the
property run is recorded as one ordinary test result.
}
\examples{
reverse_law <- new_law(
  "reverse is involutive",
  generators = list(x = gen_vector(gen_integer(), max = 8L)),
  holds = function(x) identical(rev(rev(x)), x)
)
check_law(reverse_law, tests = 20L, seed = 1L)
}
