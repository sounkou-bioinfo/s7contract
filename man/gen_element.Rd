% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generator.R
\name{gen_element}
\alias{gen_element}
\alias{gen_choice}
\title{Choose values or generators}
\usage{
gen_element(values, prob = NULL)

gen_choice(..., prob = NULL)
}
\arguments{
\item{values}{Non-empty atomic vector or list of values.}

\item{prob}{Optional finite non-negative sampling weights, one per entry,
with at least one positive weight.}

\item{...}{One or more generators, ordered from simpler to more complex.}
}
\value{
An S7 generator object. \code{gen_element()} preserves an atomic input's
element prototype; list inputs use a list prototype. \code{gen_choice()} retains
a common prototype when all branches agree, otherwise it uses list elements.
}
\description{
\code{gen_element()} samples one element of a vector or list. \code{gen_choice()}
samples a generator and draws from it at the current size. All choices are
available at size zero. Both shrink toward earlier entries, then
\code{gen_choice()} shrinks within the selected generator. Zero-weight entries
are excluded from both generation and shrinking.
}
\examples{
bases <- gen_element(c("A", "C", "G", "T"))
nullable <- gen_choice(gen_constant(NA_integer_), gen_integer(), prob = c(1, 9))
gen_example(gen_vector(bases, min = 4L, max = 4L))
}
