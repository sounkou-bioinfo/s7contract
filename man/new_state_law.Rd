% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state-law.R
\name{new_state_law}
\alias{new_state_law}
\title{Define a generative law for a stateful protocol}
\usage{
new_state_law(
  name,
  initial,
  commands,
  setup,
  teardown = function(fixture) NULL,
  max_commands = 10L
)
}
\arguments{
\item{name}{Non-empty description of the law.}

\item{initial}{Initial model with value semantics.}

\item{commands}{Non-empty list of descriptors made with \code{\link[=new_command]{new_command()}}.}

\item{setup}{Function of no arguments returning a fresh implementation fixture.}

\item{teardown}{Function of that fixture releasing its resources.}

\item{max_commands}{Maximum generated sequence length.}
}
\value{
An S7 law accepted by \code{\link[=check_law]{check_law()}} and \code{\link[=expect_law]{expect_law()}}.
}
\description{
Builds an ordinary law for \code{\link[=check_law]{check_law()}} or \code{\link[=expect_law]{expect_law()}}. Each evaluation,
including every shrink candidate, calls \code{setup()} to obtain a fresh fixture
and \code{teardown(fixture)} once after successful setup. If setup itself fails,
it is responsible for releasing any partially acquired resources.
}
\details{
A false command postcondition falsifies the law. Unexpected callback errors
or warnings produce an error and stop shrinking. Cleanup failures do not
replace an established postcondition failure: they are recorded in its
\code{cleanup_condition} and stop shrinking. User callbacks must terminate; the
shrink budget counts candidate evaluations, not individual commands.

Counterexamples retain the original and reduced command sequences. Their
\code{original_condition} and \code{condition} describe the original and reduced
failures, with the failing \code{step}, \code{command}, callback \code{phase}, pre-command
\code{model}, resolved \code{input}, \code{output}, and execution \code{trace}. Trace entries
record the model before and after each completed command. Model and output
snapshots require value semantics; mutable output handles retain their usual
R reference semantics. Replay has the same requirements as \code{\link[=check_law]{check_law()}},
and setup must reproduce the same initial implementation state.
}
\examples{
increment <- new_command(
  "increment",
  generate = function(state) gen_integer(0L, 5L),
  execute = function(fixture, input) {
    fixture$value <- fixture$value + input
    fixture$value
  },
  update = function(state, input, output) state + input,
  ensure = function(state, input, output) identical(output, state + input)
)
counter_law <- new_state_law(
  "counter follows its model", 0L, list(increment),
  setup = function() list2env(list(value = 0L), parent = emptyenv())
)
check_law(counter_law, tests = 20L, seed = 1L)
}
