% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generator-double.R
\name{gen_double}
\alias{gen_double}
\title{Generate finite double values}
\usage{
gen_double(min = -100, max = 100, origin = NULL)
}
\arguments{
\item{min, max}{Finite scalar numeric bounds, with \code{min <= max}.}

\item{origin}{Finite scalar numeric shrink target within the bounds. \code{NULL}
chooses zero when it is in range, otherwise the nearest bound.}
}
\value{
An S7 generator with a double element prototype.
}
\description{
At size zero, \code{gen_double()} draws only \code{origin}. The bounds expand linearly
from the origin to \code{min} and \code{max}, reaching the full interval at size 100.
Larger sizes use that same interval. Use \code{\link[=gen_resize]{gen_resize()}} to sample the full
range at every runner size. Within the current bounds, generation interpolates
one uniform draw from \code{\link[stats:Uniform]{stats::runif()}}. This samples a finite-precision
approximation to a continuous uniform distribution, not all representable
doubles uniformly. Bounds are inclusive constraints; endpoints are not
guaranteed to be drawn. Use \code{\link[=gen_choice]{gen_choice()}} with constants to target them.
}
\details{
Shrinking tries the origin, then the midpoint between the origin and the
generated value, then successive midpoints approaching that value. For
example, 8 with origin 0 has children 0, 4, 6, 7, 7.5, and so on. Each child
follows the same rule. Candidates remain between the origin and their parent;
iteration stops when rounding prevents further progress. Children are built
only when visited. Values close to zero can require many steps to shrink
through subnormal doubles, so the runner's evaluation budget still applies.

\code{NA}, \code{NaN}, and infinities are excluded. Add them explicitly with
\code{\link[=gen_choice]{gen_choice()}} or \code{\link[=gen_element]{gen_element()}}. Branch weights control generation and
branch order controls shrinking; zero-weight branches are excluded from both.
}
\examples{
measurements <- gen_double(-10, 10)
gen_example(measurements, size = 100L)

# 80\% finite draws; 5\% each for NA, NaN, -Inf, and Inf.
numeric_values <- gen_choice(
  measurements, gen_element(c(NA_real_, NaN, -Inf, Inf)),
  prob = c(4, 1)
)
gen_example(gen_vector(numeric_values, max = 5L))
}
\references{
Haskell Hedgehog separates shrink origins from
\href{https://github.com/hedgehogqa/haskell-hedgehog/blob/master/hedgehog/src/Hedgehog/Internal/Range.hs}{size-dependent bounds}
and uses \href{https://github.com/hedgehogqa/haskell-hedgehog/blob/master/hedgehog/src/Hedgehog/Internal/Shrink.hs}{fractional shrinking toward an origin}.
The \href{https://hedgehogqa.r-universe.dev/hedgehog/doc/manual.html}{R Hedgehog manual}
documents \code{gen.unif()} and mixtures with exceptional numeric values.
}
