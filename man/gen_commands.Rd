% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state-law.R
\name{gen_commands}
\alias{gen_commands}
\title{Generate and shrink model-valid command sequences}
\usage{
gen_commands(initial, commands, max = 10L)
}
\arguments{
\item{initial}{Initial model with value semantics.}

\item{commands}{Non-empty list of descriptors made with \code{\link[=new_command]{new_command()}}.}

\item{max}{Maximum sequence length.}
}
\value{
An S7 generator of lists of steps. Each step contains an integer \code{id},
a \code{command} name, and its generated \code{input}.
}
\description{
Sequence length is sampled from zero through \code{min(size, max)}. Generation
stops early if every command is unavailable. Available commands are selected
with equal probability. Generated inputs must satisfy their preconditions.
}
\details{
Shrinking removes contiguous chunks, then shrinks command inputs. Each
candidate is replayed against the symbolic model: commands with unsatisfied
preconditions or missing output references are removed, together with any
commands that depend on them. Retained command IDs never change. Candidate
construction is lazy and executes no implementation commands or fixtures.
}
