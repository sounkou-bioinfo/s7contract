% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generator-sample.R
\name{gen_sample}
\alias{gen_sample}
\alias{gen_subsequence}
\title{Sample source positions without replacement}
\usage{
gen_sample(values, size = length(values))

gen_subsequence(values, min = 0L, max = length(values))
}
\arguments{
\item{values}{An atomic vector or list, optionally named.
\code{NULL} is also accepted as an empty source. Classed vectors such as dates
and factors must support \code{length()} and integer \code{[} subsetting
that preserves their class and returns one entry per position. Arrays and
data frames are not supported. Length must be at most \code{.Machine$integer.max}.}

\item{size}{Fixed non-negative sample cardinality, at most \code{length(values)}.}

\item{min, max}{Inclusive non-negative subsequence length bounds, with
\verb{min <= max <= length(values)}.}
}
\value{
An S7 generator.
}
\description{
\code{gen_sample()} draws a uniform ordered sample of exactly \code{size} source
positions, without replacement. Its cardinality and sampling range do not
depend on the runner's size. The default draws permutations of the source.
Shrinking moves positions toward the beginning of the source, from left to
right. Each position tries the earliest position unused by its prefix, then
integer bisections toward its current position. Targets already in the prefix
are skipped; targets used later in the sample are swapped. Every child is
lexicographically smaller, with the same cardinality and distinct positions.
The terminal sample consists of the first \code{size} source entries in order.
}
\details{
\code{gen_subsequence()} chooses a length uniformly between \code{min} and
\code{min(max, min + runner_size)}, then samples that many positions and sorts
them. It shrinks length toward \code{min} first, rebuilding a sample with a captured
seed as in \code{\link[=gen_bind]{gen_bind()}}, then shrinks positions. Source order and length
bounds are preserved. Different position shrinks can yield the same sorted
subsequence. Setting \code{min = max = length(values)} yields a constant.

Uniqueness concerns positions, not values: duplicated source entries can
appear together. Subsetting with \code{[} preserves names and supported classes;
values themselves are not shrunk. Empty sources allow only empty selections.
Both generators have a list prototype, so \code{\link[=gen_vector]{gen_vector()}} nests their results.
For sampling with replacement, compose \code{\link[=gen_element]{gen_element()}} and \code{\link[=gen_vector]{gen_vector()}}.
}
\examples{
gen_example(gen_sample(letters, size = 3L))
gen_example(gen_subsequence(letters, max = 5L))
gen_example(gen_sample(seq_len(100000000L), size = 3L))
}
\references{
\href{https://stat.ethz.ch/R-manual/R-devel/library/base/html/sample.html}{R's sampling documentation}
describes positional sampling and the hash algorithm used for small samples
from large populations. These generators use \code{sample.int()} without
constructing a vector of every source position.
The \href{https://hedgehogqa.r-universe.dev/hedgehog/doc/manual.html}{R Hedgehog manual}
documents subsequences and sampling as distinct generator domains.
}
